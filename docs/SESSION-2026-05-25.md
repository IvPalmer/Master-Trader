# Session 2026-05-25 — full day breakdown

What changed today across the fleet. For the next session walking in cold.

## Dashboard restructure (5 rounds of codex review)

Operator opened the dashboard and said it was "kinda crappy and useless". Worked through three rounds of codex review. Net result: dashboard now reads as an ops console, not a polite SaaS panel.

- **Light theme** swapped in (warm off-white `#fafaf7` / `#f5f4ee`, deep teal accent `#0891b2`, no pure black/white per taste-skill rules)
- **Live tab restructured**: command bar `INCIDENTS · N ACTIVE` at top (expandable rows, dismiss/check-logs actions), flat 7-cell fleet-vitals strip (NOT card grid), empty open-positions slot replaced with Last Closed Trade card so nothing is dead space
- **Dry-run tab restructured**: graduation board table (bot · blocker · trades progress · days progress · PF · DD · last trade) + drawer-style single-dossier below table. Click row → swap dossier inline. `open detail →` routes to per-bot tab for full surface.
- **Per-bot detail restructured**: 1-line identity strip (deleted the 6-tile metric repetition) + 3-panel diagnostic cluster at TOP (last-trade-trace / open-trade-status / live-vs-backtest deviation) before any charts. Deleted the "strategy signals — data missing" trust-killer row.
- **Per-bot tabs preserved** in nav (operator explicit preference — never collapse). Each tab has LIVE/DRY badge.
- **Killers observational tab removed** (the SQLite paper-sim). Only `killers-scalp` (Freqtrade Futures dry-run) remains.

Bugs fixed in the pass:
- C1 fleet stats now actually fleet-aggregated (was falling back to `live[0]`)
- C2 `.tag.live` is green (was red — false alarm everywhere)
- C3 status-pill `pulse` animation gated to warn/err only (was animating on green)
- Double-percent `%%` rendering
- Partial-payload safety chains across hero getter
- `toMs()` helper for ms-vs-seconds timestamp coercion (markpoints land on equity curve now)
- `pfDelta` numeric compare instead of string-toFixed
- Real stop label from `bot.baseline?.stoploss` instead of hardcoded `-5%`
- Dead `killers-sim` CSS removed (~60 lines)
- Chart `COLORS` palette retuned to match light theme

## KillersScalp webhook fix (Telegram alerts now work end-to-end)

`KillersScalpV1.json` webhook was missing `"format": "json"` — so Freqtrade sent form-encoded payloads, `trade-webhook` rejected with 400, no Telegram alerts went out. Confirmed by checking trade-webhook + ft-killers-scalp logs at 12:32:23 UTC (the POL #2143 entry time). Added `"format": "json"`, recreated `ft-killers-scalp` with `--no-deps`. Open POL trade survived the restart (persistent sqlite). Two manual smoke tests via `/test/notify` came back `telegram_sent:true`.

POL/USDT:USDT trade still open from yesterday at +6.5%.

## Insiders Scalp replay validated (parser bugs fixed, evidence ready for Eduardo)

Codex audit found 5 substantive parser bugs in the insiders-bridge simulator:
- F1 `increase` events recorded but not enacted (no notional change, no avg-entry recompute)
- F2 trades without initial SL silently dropped even when a later message brought one
- F3 backwards-TP trades accepted (LONG with TP < entry, etc.)
- F4 realized vs unrealized PnL mixed
- F5 no per-trade match against the channel's published recap posts

All five fixed. Code lives in `ft_userdata/insiders_bridge/`:
- `llm_simulator.py` — added `SL_BACKFILL_WINDOW_HOURS=24` + `pending_sl` flag + direction-aware management matching + `unrealized_pnl_mark` separate field
- `recap_matcher.py` — NEW, extracts per-trade lines from the channel's two weekly recap posts and matches them greedy by (symbol, direction, ±48h window) against sim trades
- `_local/weex.py` — fixed to use `/capi/v2/market/historyCandles` not `/market/candles` (the old endpoint silently returned latest candle for every query, causing 10 BTC trades to share entry $77,048 across months). Added Binance Futures `/fapi/v1/klines` as PRIMARY price source with WEEX fallback for unsupported symbols (XTIU, XAG). 2-min candle-drift sanity check raises on staleness.
- `.gitignore` split — `_local/*.py` code IS tracked now, only data + secrets (.session, .env, *.json exports) stay ignored.

### Honest numbers post-fix (replay against Eduardo's 1,087-message export, $1k risk-budget account, 83 days):

| metric | value |
|---|---|
| messages classified | 1,087 / 1,087 (100%) |
| actionable signals | 431 |
| trades materialized | 146 |
| trades sized | 102 (44 honestly skipped) |
| trades closed | 67 (35 still open at export cutoff) |
| **win rate ($)** | **52.0%** |
| **profit factor** | **1.79** |
| **realized PnL** | **+$232.70** |
| **account return** | **+23.27% over 83 days** |
| avg leverage | 9.4× (matches Eduardo's $10/SL% rule) |
| BTC 77048-collision trades | 0 (was 10 pre-WEEX-fix) |

### Channel recap apples-to-apples (Feb 16-24 window — the one comparable in the export):

| | sim | channel recap |
|---|---|---|
| trades | 16 (8W/7L/1E) | 14 (7W/7L) |
| WR | **50.0%** | **50.0%** ✓ exact match |

Validates lifecycle parsing. PnL differs because of position sizing (sim uses 1% per trade, channel says 2-5%).

Draft message for Eduardo lives at `docs/insiders-signals/eduardo-summary-2026-05-25.md`. NOT sent yet — operator told me to hold any outbound message until explicit approval.

## Operational notes

- Worktree cleanup complete (4 worktrees from today removed)
- All commits pushed to `origin/vps-deploy`
- Last commit: `fc2b976` — tracked insiders code under `_local/`
- ft-dashboard, ft-killers-scalp recreated today with `--no-deps` (live POL trade preserved)
- New memory: `feedback_no_outbound_until_told.md` (no third-party messages without explicit go-ahead) + `feedback_dashboard_per_bot_tabs.md` (every bot gets its own nav tab, never collapse)

## Single remaining blocker for Insiders to go live

Eduardo's Telethon `.session` file. Drop at `ft_userdata/insiders_bridge/_local/eduardo.session` and the listener subscribes to the channel. Every other pipeline stage is green.
