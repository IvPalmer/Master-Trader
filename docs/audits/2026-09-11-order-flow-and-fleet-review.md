# Order flow paper and fleet review — 2026-09-11

The fleet is running, but execution remains fragile and no current strategy has enough evidence to establish a durable profitable edge. The useful next step is to repair exit execution, reconcile portfolio accounting, and collect clean evidence from the existing small allocations. The paper supports a separate order-flow research experiment; it does not justify increasing leverage, loosening entry gates, or changing ROI ladders today.

The original request was to review [OrderFlowpaper.pdf](/Users/palmer/Downloads/OrderFlowpaper.pdf), the strategies, and their configurations. The subsequent instruction to proceed authorized local fixes. The paper was treated as research evidence, not as operational instructions. Production observations below were collected read-only on **September 11, 2026, approximately 20:15–20:18 UTC**. The source checkout and VPS both started at `8226067`. Subsequent changes described here are **local only**: no deployment, restart, order, transfer, or automatic rearming of existing exits occurred. Source links point to the working copy, which now contains some fixes absent from production.

The paper studies signed buying and selling activity across 82 cryptocurrencies from January 2018 through June 2022. Its daily sample excludes weekends and US holidays; its weekly observations include seven days. The 82-coin panel requires continuous nonzero trading throughout the sample and market capitalization above $1 million at its end. That selection excludes failed or discontinued assets using future information. It is a survivorship-selected research universe, not a point-in-time investable portfolio. The recursive forecast evaluation runs from **February 18, 2020 through June 30, 2022**, following separate training and validation periods and monthly updating. These distinctions matter when interpreting its results. See paper pages 9–11 and 20–21.

Its order flow is the log difference between buyer-initiated and seller-initiated transaction volume, standardized by recent flow volatility. CryptoCompare supplies signed volume across more than 300 exchanges and eleven fiat currencies: the G10 currencies plus Korean won. This is different information from candle direction, total volume, open interest, funding rates, or an order-book imbalance snapshot. Binance USDT flow could test a related mechanism, but would not reproduce the paper's international currency structure.

The strongest practical insight is the distinction between daily and weekly signals. At the daily horizon, raw aggregate flow produced a weak long-short sort; removing the component associated with same-period returns improved the result. The paper constructs that residual recursively using only information available at the decision time. Weekly raw flow worked more convincingly without this adjustment. This favors starting with two simple, separately registered hypotheses: daily residual flow and weekly raw flow. It does not favor immediately searching a large machine-learning model family. See pages 26–27 and Table 7.

The headline statistics need their accompanying risk numbers:

| Paper result | What it establishes and what it does not |
| --- | --- |
| Best daily out-of-sample forecast R²: **0.66%**, stochastic gradient boosting, Table 6 | A small improvement in pooled prediction error; not 66% accuracy or a forecast of account returns. |
| SGB order-flow long-short Sharpe: **3.63**, Tables 8–9 | A historical portfolio result requiring both sides, rebalancing, and feasible execution. |
| SGB long-only Sharpe: **1.85**, maximum drawdown **68%**, Table 9 | Removing shorts materially changes the risk profile. This is not a stable capital-preservation strategy. |
| Nonlinear ensemble long-only Sharpe: **1.88**, maximum drawdown **66%**, Table 9 | Agreement across models does not remove severe market exposure. |

The paper reports gross returns and break-even transaction-cost estimates, with comparisons to assumed trading costs. Those are not our realized fees, queue positions, fills, funding, borrow availability, or losses during failed exits. Its findings motivate testing; neither the paper's period nor its selected universe establishes a 2026 deployable edge.

The fresh fleet snapshot separates current measurement epochs from retired histories:

| Bot | Mode | Current closed-trade evidence | Assessment |
| --- | --- | --- | --- |
| FundingFade | Live | 5 trades; 4 wins, 1 loss; **+$0.9665**, PF **2.39** | Encouraging but far too small to infer stability or scale. |
| KeltnerBounce | Live | 0 closes | Current profitability is unmeasured. |
| Killers | Live | 0 current-epoch closes; 3 open positions | Execution reliability is the immediate issue. Older profitable results belong to a different epoch. |
| OITrendPullback | Dry-run | 8 trades; 3 wins, 5 losses; **+$0.5619**, PF **2.57** | Simulated, tiny, payoff-sensitive evidence; keep in research. |
| ShortKeltner | Dry-run | 0 closes | No current profitability evidence. |
| Insiders | Dry-run | 0 closes | No current profitability evidence. |

Killers held **19 DOT, 11 GRAM, and 65 TRX**. Every receiver ladder had its first target rejected and later targets pending. A direct Hyperliquid check found three reduce-only native **stop-limit** orders and **zero take-profit orders**. Marked position notional was approximately **$56.91**, against account value **$101.68** and margin used **$45.81**. Thus the positions had exchange-resident stops, but their intended take-profit execution was absent. Stop-limit orders can remain unfilled after a price gap; their presence does not establish a hard maximum loss.

The deployed receiver divided the entry quantity equally across all targets without ensuring that each slice survived venue precision and minimum-notional checks. A failed first placement became terminal, while reconciliation processed active orders only. Hyperliquid documents a $10 minimum-notional rejection and distinct invalid-price, margin, reduce-only, and liquidity errors; these require different handling. See the [official error definitions](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/error-responses). The local [receiver](/Users/palmer/Work/Dev/master-trader/services/killers-receiver/app/main.py:975) and [target planner](/Users/palmer/Work/Dev/master-trader/services/killers-receiver/app/tp_plan.py:33) now construct executable allocations. Ladder creation is serialized; submission intent is persisted before sending an order. Confirmed throttling and failed reads can retry after a cooldown. Ambiguous submissions are reconciled against observed orders, including immediate fills, before further action. An unresolved submission defers a competing partial close. Explicit TP1 instructions retain the original signal target. Existing rejected and manually cancelled ladders are not automatically rearmed.

The latest six-hour Killers log window contained approximately **180 `Unable to exit trade` warnings associated with `RateLimitExceeded`**. This demonstrates degradation of the management path, not 180 distinct failed sell instructions. Hyperliquid shares an aggregated REST weight budget per IP, weights endpoint types differently, and separately limits address actions. Multiple individually rate-limited clients can still overload a shared budget. Add endpoint-weight accounting, latency and retry telemetry, shared throttling, bounded backoff, and priority for managing existing exposure. A websocket or shared cache may reduce duplicated reads after its behavior is measured. See [Hyperliquid's rate-limit documentation](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/rate-limits-and-user-limits). This infrastructure work remains unresolved.

Portfolio accounting also needs correction before risk percentages are trusted. FundingFade and Keltner use the same Binance credential internally. Each reported `total_bot` near **$90.3356**, while their reported starting values were approximately **$89.3788** and **$90.3356**. Neither has an explicit `available_capital` allocation. The exporter sums starting capital by live bot at [metrics_exporter.py:276](/Users/palmer/Work/Dev/master-trader/ft_userdata/metrics_exporter.py:276), effectively counting the shared wallet twice. Fix the account-level denominator and explicit allocation ledger; do not merely hide one bot's P&L. Cross-bot losses, equity marks, cash movements and protection budgets must reconcile to unique exchange accounts.

A further discrepancy needs investigation: exchange leverage settings for DOT/GRAM/TRX were **1/1/2**, while Freqtrade recorded approximately **1.60/1.57/2.22**. Freqtrade attributed about **$29.56 stake** to these positions, versus **$45.81 margin used** at Hyperliquid. This is a reconciliation issue, not a reason to change leverage automatically. Compare actual size, entry price, mark, margin, liquidation reference, and stop calculations before treating either leverage display as authoritative. At maximum configured slots, the live fleet requests roughly **$100 committed capital** and **$200 notional**, with approximately **$12.55 nominal stop risk** under configured assumptions. These describe requested configuration capacity, not current exposure, a guaranteed exchange-margin bound, or a hard loss cap. Resolve the discrepancy before scaling.

For the autonomous strategies, data correctness is more useful than parameter tuning. FundingFade's BTC macro freshness check originally considered only the final funding event, allowing a later recovery to hide interior data outages. A three-day gap reproduced 47 stale hourly bars incorrectly remaining valid. The local fix checks the observation available to each bar at [FundingFadeV1.py:313](/Users/palmer/Work/Dev/master-trader/ft_userdata/user_data/strategies/FundingFadeV1.py:313). Its economic settings are unchanged. Earlier headline backtests predate some current reversal/exit rules and should not be presented as validation of this revision.

OITrend could accept a baseline nearly two hours old as its nominal 45-minute change. The local fix uses exchange observation timestamps, bounds the baseline to the intended window plus one five-minute sampling interval, and expires confirmation from observation time. Current OI is attached only to the latest live/dry decision, never copied over historical candles; historical runs do not call the live endpoint. See [OITrendPullbackV1.py:140](/Users/palmer/Work/Dev/master-trader/ft_userdata/user_data/strategies/OITrendPullbackV1.py:140). Its earlier attractive calibration was withdrawn after timing, concurrency, exit, venue, and cost mismatches; the corrected study reported PF 0.74 and a 4% loss. Eight new simulated trades do not overturn that finding.

Keltner should retain a frozen universe and exit contract while accumulating evidence. Its July post-selection comparison showed weaker results after expanding beyond the validated universe, and the validated subset itself was inconclusive. The prior [BVC-VPIN experiment](/Users/palmer/Work/Dev/master-trader/docs/vpin_keltner_veto_2026-04-21.md) failed held-out and walk-forward checks. BVC infers buying and selling from price changes; it is not the signed transaction flow used in this paper. Do not revive that veto under an order-flow label. ShortKeltner's reported 0.20 signals/month is a Binance-proxy frequency diagnosis, not a Hyperliquid profitability backtest. Insiders currently uses the shared Killers executor; legacy `InsidersScalpV1` defects should not be mislabeled as active faults.

The proposed **OrderFlowResidualRankV1** is a research plan only; no strategy or collector is being deployed:

1. **Build a cheap, auditable flow dataset.** Binance spot klines expose total quote volume and taker-buy quote volume. Let buyer-initiated quote volume be `B` and seller-initiated volume be `S = total_quote_volume - B`; test `log(B) - log(S)` with a prespecified zero-volume policy. Retain raw fields, close times, retrieval times and quality flags. Never infer transaction direction from a green/red candle. This is a single-venue USDT proxy, not G11 world flow. Validate the decomposition against aggregate trades on a small sample. The fields are documented in [Binance's market-data API](https://developers.binance.com/docs/binance-spot-api-docs/rest-api/market-data-endpoints).

2. **Register two simple baselines.** Daily: rank the component of standardized flow unexplained by recent returns, with regression coefficients and scaling estimated only on permitted training/history. Weekly: rank raw standardized flow. Also retain return-only and equal-weight-universe controls. Freeze the normalization, residualization window, missing-data rule, ranking and holding period before inspecting the final holdout. Keep model-family search out of the first experiment.

3. **Preserve causal timing and investability.** Use closed UTC bars and trade only after their data could have arrived. Store historical universe membership, listing/delisting dates and liquidity eligibility; do not select today's survivors retrospectively. Specify how suspended markets and delistings exit. Explicitly distinguish a seven-day crypto calendar from the paper's business-day daily sample. Fit preprocessing only on training data, use separate validation for choices, and preserve an untouched chronological test period. Rolling refits must follow the frozen schedule.

4. **Evaluate a portfolio, not just predictions.** Measure net returns, drawdown including open marks, turnover, exposures, concentration and confidence intervals. Compare long-only results with equal-weight exposure to the same available universe and with market/return controls, so beta is not called order-flow skill. A long-short diagnostic is optional and must not assume unavailable shorts, free borrowing, or absent funding. Reproduce actual slots, capital, quantity precision and order minima. Stress fee tiers, spreads, slippage, delayed arrivals, maker nonfills and order timeouts; use detail bars where exits depend on intrabar paths.

5. **Keep the decision falsifiable.** Require improvement over frozen controls after realistic costs across multiple chronological market regimes, not just a positive gross Sharpe or one profitable slice. Reject the candidate if the result disappears with realistic fills, delisted assets, or modest timing delay. Only then consider an explicitly bounded shadow observation period; live admission would require a separate review. Testing signed flow as a FundingFade/Keltner quality feature should be a separate incremental experiment against their unchanged baselines.

Do not blanket-enable Freqtrade's full order-flow processing on the fleet to run this research. Its documentation labels the feature beta, notes larger raw-trade downloads and memory usage, and does not support combining it with FreqAI as a tested workflow. A separate compact aggregation pipeline is a better first experiment on this already rate-limited stack. See [Freqtrade's order-flow documentation](https://www.freqtrade.io/en/stable/advanced-orderflow/).

Practical sequencing is: finish and independently review executable TP planning/recovery; reconcile the three existing positions without duplicating exits; fix unique-account capital accounting and leverage interpretation; instrument shared rate limits; then collect frozen strategy evidence and the new shadow dataset. Preserve current small risk budgets and ROI settings. Positive results on five or eight trades do not justify scaling.

Local work also includes startup-mode guards, monitoring/alert formatting, and dashboard changes that expose TP execution faults and derive booked quantities from filled orders. The [dashboard execution-health path](/Users/palmer/Work/Dev/master-trader/ft_userdata/ft_dashboard/app.py:388) is intended to prevent a reachable bot from appearing operationally healthy when exits are blocked. These changes remain separate from the unresolved shared-wallet and exchange-throttling issues.

Validation at drafting: the focused funding/OI and fleet-contract suite passed **69 tests**, including 16 new regression cases; whitespace checks passed. This uses isolated import stubs and does not establish exchange integration or profitability. Final coordinated verification: **195 receiver tests passed**, including 34 new TP reliability cases; **61 dashboard tests passed**. The broader configuration/guard/fleet suite produced **127 passed, 1 skipped, and 2 pre-existing failures**: a retired local-compose restart-policy assertion and a missing generated Grafana dashboard directory. Python compilation and whitespace checks passed. Tests use mocked exchanges and some Freqtrade import stubs; the new code has not been exercised by a live exchange deployment. No production change is implied by passing local tests.

A second read-only check at **22:44 UTC** confirmed the same three filled quantities and obtained the executor's actual precision metadata (DOT step 0.1; GRAM/TRX step 1). Running the new planner locally against the persisted targets yields this concrete recovery proposal:

| Position | Proposed first executable group | Proposed final group | Total allocated |
| --- | --- | --- | --- |
| DOT | 10.5 DOT at $1.06 (original target 5), $11.13 notional | 8.5 DOT at $1.40 (target 9), $11.90 notional | 19 DOT |
| GRAM | 6 GRAM at $1.80 (target 5), $10.80 notional | 5 GRAM at $2.25 (target 8), $11.25 notional | 11 GRAM |
| TRX | 32 TRX at $0.39 (target 4), $12.48 notional | 33 TRX at $0.50 (target 8), $16.50 notional | 65 TRX |

These are **unsubmitted proposals**, not resting orders or forecasts. Grouping preserves total quantity and does not bring any allocation forward to an earlier target; it **delays some partial exits** until a later original target makes their combined quantity executable. This changes the realized exit schedule and must be considered in future strategy evaluation. No capital is added to accommodate the order minimum. The proposal must be recomputed if quantities, targets, open orders, precision, or venue rules change. Before deployment/recovery, take a receiver database backup, reconcile current exchange orders and actual fills, preserve native stops, and review the existing rejected rows individually. The local code deliberately does not replace them on startup.

## Deployment record — 2026-09-12 00:20–00:30 UTC

Independent review: codex was unreachable (usage limit until 2026-09-12 00:36 UTC), so the diff was reviewed by a separate Claude subagent instead. It confirmed the lock discipline, the single-submission recovery path, the planner arithmetic, the migration, the FundingFade mask, and the compose `set -e` semantics, and returned one blocking finding, which was fixed before deployment: an executor 5xx on a submission left the row `unknown` forever, and an `unknown` row freezes the ladder and defers every later channel partial. Freqtrade returns 500 for a ccxt `InvalidOrder`, so this was the realistic outcome of the very first re-arm. The receiver now compares the snapshot with the persisted `prior_order_ids`: an unidentified new exit-side order keeps the row `unknown`; a snapshot proving nothing was created moves it to `retry`, which the reconciler resubmits after the 60-second cooldown. The clock tolerance between `submitted_at` and exchange order timestamps was widened from 1 s to 5 s. Non-blocking notes kept for later: a Freqtrade-initiated cancel (exit timeout is 30 days for this bot) freezes the ladder while the dashboard reports it healthy; the re-arm has no price-side pre-flight, so confirm targets sit above the mark for longs; the OITrend baseline window equals one poll interval and rejects any observation ahead of the local clock. A local simulation of the DOT position (rejected + pending rows, real precision metadata) confirmed: no automatic action on the stuck ladder after deployment; after deleting its rows exactly one submission (10.5 DOT at 1.06) across repeated ticks of both reconcilers.

Commit `533bc57` on `vps-deploy`, pulled on the VPS at 00:20 UTC. Receiver databases were backed up to `/home/ubuntu/master-trader/state/backups/` before any container change. Order of operations: build `killers-receiver` and `ft-dashboard` images; recreate `killers-receiver`, `insiders-receiver`, `ft-dashboard` with `--no-deps`; restart `metrics-exporter`, `fundingfadev1` (0 open trades) and `oi-trend-pullback`; after confirming the db-mode guard passes inside each Hyperliquid container, recreate `ft-killers-scalp`, `ft-insiders-scalp`, `ft-short-keltner-hl-live` with `--no-deps` (no TP ladder was active, so the no-reload-mid-ladder rule did not apply). All five restarted bots came back `running` with the expected strategy and mode; Killers still shows DOT 19 / GRAM 11 / TRX 65 with one open native stop each. The receiver migration added `submitted_at` and `prior_order_ids`; the ladder rows are unchanged (3 rejected, 22 pending) by design. The exporter now scrapes six bots including ShortKeltnerV2HL. The dashboard reports Killers execution health **critical** for all three positions (`blocked`: rejected + pending, no active order).

Killers logged roughly 1,300 `RateLimitExceeded` warnings in the four minutes after its recreate (order-history refresh at startup), falling to zero within six minutes. The persisted log shows hourly bursts of 100–900 before the recreate, so the shared-rate-limit work in this review remains open and unchanged by the deployment. An unmanaged container `ft-short-keltner-hl` (no compose label, up two months) is still running beside the compose-managed `ft-short-keltner-hl-live`; it was left alone.

**Not done, by design: the three ladders are still stuck.** Re-arming them submits reduce-only limit exits, so it is left to the operator. The script `deploy/vps/rearm-killers-tp-ladders.sh` backs up the database, refuses positions with live, uncertain or exchange-linked rows, deletes the stuck rows, and the delayed-fill reconciler rebuilds each ladder with the planner within about a minute. At 00:27 UTC the marks were DOT 1.0451, GRAM 1.3634, TRX 0.33858, all below their first executable targets (1.06, 1.80, 0.39), so no submission would fill as a taker immediately; DOT is within 1.5% of its first target. Recompute if quantities, targets or open orders change.
